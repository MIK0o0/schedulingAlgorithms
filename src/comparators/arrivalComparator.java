package comparators;

import java.util.Comparator;
import com.company.*;
import com.company.Process;
/*
public class arrivalComparator<Process> implements Comparator<Process> {
    @Override
    public int compare(Object process1, Object process2) {
        int tym;
        tym = (Process)process1.getArrivalTime();
        if (tym == 0){
            return 0;
        }else if (tym > 0){
            return 1;
        }else {
            return -1;
        }
    }

 
}

 */
